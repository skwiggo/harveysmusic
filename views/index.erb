<h1>Current Stock Inventory:</h1>

<!-- <table style="width:100%">
  <tr>
    <th>Artist Name</th>
    <th>Album Name</th>
    <th>Product Stock Level</th>
    <th>Stock Check</th>
  </tr>
  <tr>
    <td><%= @artists.each {|artist| artist.name} %></td>
    <td><%= @albums.each {|album| album.name} %></td>
    <td><%= @stocks.each {|stock| stock.stock_level} %></td>
    <td><%= @stocks.each {|stock| stock.stock_check} %></td>
  </tr>

</table> -->

<div>
  <% @artists.each do |artist| %>
    Artist: <%= artist.name  %><br><hr>
  <% end %>
  <% @albums.each do |album| %>
    Product: <%= album.name  %><br><hr>
  <% end %>
  


</div>